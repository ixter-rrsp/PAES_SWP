// How many of EACH source (announcements, events) fetch on first
// paint — the rest load lazily in batches as the visitor scrolls,
// instead of pulling every announcement and event up front.
export const NEWS_EVENTS_PAGE_SIZE = 10;
